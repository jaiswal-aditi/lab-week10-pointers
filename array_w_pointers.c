/*
Write a C program that:
1. allows the user to provide the number of elements in an array.
2. allows the user to provide the elements of the array and store them in a dynamically allocated array.
3. Uses pointers to:
  o Print the array in the order entered.
  o Print the array in reverse order.
  o Sort the array in ascending order.
  o Swap n elements of the array in cyclic order
  - And displays the outputs for each of these conditions.

** See PDF for sample input/output and detailed requirement.
*/
